import { RequestForm } from "@/components/request-form";
export default async function RequestPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams;return <main className="page"><div className="container"><div className="form-card"><p className="eyebrow">Sin cuenta y sin costo</p><h1 style={{fontSize:"clamp(2.3rem,6vw,4rem)"}}>Publica lo que necesitas</h1><p className="muted">Cuéntanos el trabajo. En esta versión el equipo de Tequit podrá revisar la solicitud y ayudarte a encontrar opciones.</p><RequestForm targetProvider={sp.provider} targetBusiness={sp.business} defaultService={sp.service}/></div></div></main>}
