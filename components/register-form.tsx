"use client";
import { FormEvent, useState } from "react";
import { CheckCircle2 } from "lucide-react";
export function RegisterForm(){const[done,setDone]=useState(false);function submit(e:FormEvent){e.preventDefault();setDone(true)}if(done)return <div className="success"><CheckCircle2/><h3>Perfil demo creado</h3><p>Tu cuenta inicia en plan Free y puedes publicar hasta 5 servicios. En Supabase local, el correo se vincula con Auth.</p><a className="btn btn-dark" href="/login">Ir a iniciar sesión</a></div>;return <form className="form-grid" onSubmit={submit}><div className="form-group full"><label>Tipo de cuenta</label><div className="filters"><label className="chip active"><input type="radio" name="type" value="provider" defaultChecked/> Prestador</label><label className="chip"><input type="radio" name="type" value="business"/> Negocio</label></div></div><div className="form-group"><label htmlFor="name">Nombre</label><input className="field" id="name" required/></div><div className="form-group"><label htmlFor="profession">Profesión principal</label><input className="field" id="profession" placeholder="Ej. Plomero" required/></div><div className="form-group"><label htmlFor="email">Correo</label><input className="field" id="email" type="email" required/></div><div className="form-group"><label htmlFor="phone">WhatsApp</label><input className="field" id="phone" inputMode="tel" required/></div><div className="form-group"><label htmlFor="zone">Zona</label><input className="field" id="zone" placeholder="Tepic y Xalisco" required/></div><div className="form-group"><label htmlFor="password">Contraseña</label><input className="field" id="password" type="password" minLength={8} required/></div><div className="form-group full"><label htmlFor="bio">Sobre tu trabajo</label><textarea className="field" id="bio" required/></div><div className="form-group full"><label htmlFor="firstService">Primer servicio</label><input className="field" id="firstService" placeholder="Ej. Reparación de fugas" required/><p className="help">Podrás publicar hasta 5 servicios con el plan Free.</p></div><button className="btn btn-primary form-group full">Crear cuenta y publicar perfil</button></form>}
