"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { Plus, ToggleLeft } from "lucide-react";
import { providers } from "@/lib/demo-data";
export function ServiceManager(){const initial=providers[0].services;const[items,setItems]=useState(initial);const[error,setError]=useState("");async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");if(items.length>=5){setError("Llegaste al límite de 5 servicios del plan Free.");return}const form=e.currentTarget;const name=String(new FormData(form).get("name")??"");const res=await fetch("/api/provider/services",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,plan:"free",activeCount:items.length})});const body=await res.json();if(!res.ok){setError(body.error);return}setItems([...items,{id:body.id,slug:body.id,name,category:"Otro"}]);form.reset()}return <><div className="section-head"><div><strong>{items.length} de 5 servicios usados</strong><p className="muted">Tu plan Free permite cinco servicios publicados.</p></div></div><div className="result-list">{items.map(s=><div className="lead-card" key={s.id}><div><h3 className="card-title">{s.name}</h3><span className="status" style={{background:"#e6f3eb"}}>Activo</span></div><button className="btn btn-secondary"><ToggleLeft size={18}/>Desactivar</button></div>)}</div><form className="form-card" onSubmit={add} style={{margin:"30px 0 0"}}><h3>Publicar otro servicio</h3><label htmlFor="service-name">Nombre del servicio</label><input className="field" id="service-name" name="name" placeholder="Ej. Concreto estampado" minLength={3} required/><button className="btn btn-primary btn-block" style={{marginTop:12}}><Plus size={18}/>Agregar servicio</button>{error&&<div className="empty" style={{marginTop:16}}><strong>{error}</strong><p className="help">Tu profesión y capacidades siguen apareciendo en búsqueda.</p><Link className="btn btn-dark" href="/dashboard/plan">Conocer Tequit Pro</Link></div>}</form></>}
