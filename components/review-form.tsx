"use client";
import { FormEvent,useState } from "react";
export function ReviewForm({token}:{token:string}){const[done,setDone]=useState(false);const[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/reviews",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,customerName:form.get("customerName"),rating:Number(form.get("rating")),comment:form.get("comment"),website:form.get("website")})});const body=await response.json();if(response.ok)setDone(true);else setError(body.error)}if(done)return <div role="status"><h2>Gracias por compartir tu experiencia</h2><p>La reseña será visible después de que Tequit la revise.</p></div>;return <form className="form-grid" onSubmit={submit}><label>Tu nombre<input className="field" name="customerName" required minLength={2}/></label><label>Calificación<select className="field" name="rating" defaultValue="5"><option value="5">5 estrellas</option><option value="4">4 estrellas</option><option value="3">3 estrellas</option><option value="2">2 estrellas</option><option value="1">1 estrella</option></select></label><label>Comentario<textarea className="field" name="comment" required minLength={10}/></label><input name="website" hidden tabIndex={-1}/>{error&&<p role="alert">{error}</p>}<button className="btn btn-primary">Enviar reseña</button></form>}
